# Local Postgres (host machine)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/istu_dwa_lb_2

# In Docker Compose the api service sets DATABASE_URL to host `db` automatically.
# PORT defaults to 3000.
